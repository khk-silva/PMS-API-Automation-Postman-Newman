# PMS-API-Automation-Postman-Newman
Automated SQL-driven APIs using Postman with environment variables and JavaScript test scripts to extract totals and compute a grand total, executing the suite via Newman CLI with console and HTML reports.
